<?php
/**
 * @file
 * Code for the main module.
 */

/**
 * Implements hook_menu().
 */
function custom_main_menu() {
  $items = array();

  $items['test'] = array(
    'title' => 'test',
    'description' => 'A test menu item',
    'weight' => 16,
    'type' => MENU_CALLBACK,
  );

  return $items;
}

function custom_main_test() {
  var_dump('test');
  dsm('test');
}
